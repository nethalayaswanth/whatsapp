import PreviewDialog from "./dialog";
import PreviewModalWrapper from "./wrapper";

const Preview = () => {
  return (
    <PreviewDialog>
      <PreviewModalWrapper/>
    </PreviewDialog>
  );
};

export default Preview;
